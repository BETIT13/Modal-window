"use strict";
// Manipulate the 3 button elements
// Manipulate the Popup and Overlay
// Keypress Event using the addEventListener

// The first thing to do during manipulation is to select the Elemment

const btnOpenModal = document.querySelectorAll(".btn");
const btnCloseModal = document.querySelector(".close-btn");
const OverlayEl = document.querySelector(".overlay");
const popupEl = document.querySelector(".popup");
